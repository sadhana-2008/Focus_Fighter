services:
  - type: web
    name: focus-fighter
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn --worker-class eventlet -w 1 main:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
